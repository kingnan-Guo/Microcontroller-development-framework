#CMakeLists.txt
cmake_minimum_required(VERSION 3.28)
# cmake最低版本需求，不加入此行会受到警告信息

project(Microcontroller-development-framework)
# 项目名称，同时会自动生成 PROJECT_NAME 变量，
# 使用${PROJECT_NAME} 即可访问到 Microcontroller-development-framework。


# AUX_SOURCE_DIRECTORY(. SRC_LIST) 
# 把当前目录(.)下所有源代码文件和头文件加入变量SRC_LIST

# ADD_EXECUTABLE(${PROJECT_NAME} ${SRC_LIST}) # 生成应用程序 hello

include_directories(src)
aux_source_directory(src SRC_LIST)

ADD_EXECUTABLE(main main.c ${SRC_LIST1} ) #生成应用程序 hello

